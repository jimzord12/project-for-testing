# I011 consent-gated analysis API

- Date/time and timezone: 2026-07-13, local Hermes worker session
- Tool and version: Hermes Agent / Codex CLI worker in Windows Git Bash
- Model and reasoning/effort setting: gpt-5.5 via openai-codex, default worker settings
- Issue and branch: I011 on `01-hermes-kanban-test`
- Starting commit: `1f9fab2de6ed2818a501e0bd83a97018c01c529c`
- Ending commit: uncommitted implementation diff for task `t_98427cb3`

## Hypothesis

A Hermes Kanban implementation worker can add the optional AI analysis endpoint as one vertical slice using the existing I002 score seam, I010 provider seam, and I012 safety service.

## Expected behavior

The worker should read the issue/spec context, add failing tests first, implement strict analysis processing and HTTP routing, run the phase gate, update status docs, and hand off a structured `WORK_SUBMISSION` for independent review.

## Prompt and workflow

Task `t_98427cb3` requested I011: `POST /api/v1/assessments/analyze` with explicit AI consent, 32 KiB JSON request limit, server-side deterministic recomputation, safety screening before scoreability shortcuts, strict provider output validation, application-owned narrative scoring, evidence validation, and typed fallback states.

Workflow used:

1. Loaded Kanban task context and repository specs.
2. Added failing analyze-service and analyze-route tests.
3. Implemented `src/app/api/v1/assessments/analyze/analyze-service.ts` and `route.ts`.
4. Ran focused tests, full test suite, typecheck, build, and source scan.
5. After reviewer rejection, added missing route-level union coverage and direct provider-output
   rejection coverage, then reran the same verification gate.
6. After a second reviewer rejection, changed the narrative request contract to strict canonical
   N01/N02 field objects and added a regression test for noncanonical field ids before safety,
   scoreability, or provider prompt construction.
7. After a third reviewer rejection, replaced broad/passthrough analyze response schemas with
   strict response-union schemas and parsed successful route bodies through that contract before
   returning them.
8. Task `t_971c0b37` moved the exported strict response union and all per-variant response schemas
   into the shared analyze service contract module so both the route and tests import the same
   contract layer.

## Observations

- Product implementation: the endpoint now uses consent-gated processing, the I012 safety classifier, the I010 structured generation seam, strict Zod provider output schema, application-side evidence validation, delimiter-sanitized prompt construction, and `calculateNarrativeScore` ownership.
- Rework observation: reviewer-Cron correctly caught acceptance-test gaps even though the core gate was green; route tests now exercise all four response unions, and direct processing tests now cover markdown rejection, invalid rubric values, and provider-supplied aggregate scores.
- Rework observation: reviewer-Cron also caught that arbitrary narrative field ids could bypass the
  canonical N01/N02 field contract. The request schema now rejects unknown narrative field ids before
  safety classification, scoreability shortcuts, provider prompt construction, or evidence matching.
- Rework observation: reviewer-Cron caught that the exported route response contract was still broad
  and passthrough. The route now exports strict deterministic-result, completed-analysis,
  safety-message, not-scored, and unavailable schemas, rejects unknown response keys, and parses
  successful service bodies before returning HTTP JSON.
- Contract-layer observation: the response schemas were strict but still owned by the route module.
  Moving them into `analyze-service.ts` makes the request, provider-output, deterministic-result,
  completed-analysis, and response-union contracts importable by service code, route code, and tests
  without duplicating schema definitions.
- Privacy behavior: route/service tests verify raw narrative snippets and prompt/model details are not echoed in returned fallback/safety states.
- Workflow observation: `write_file`/`patch` edit hooks again reported transient TypeScript diagnostics for Windows-native files under `src/app/api/v1/assessments/analyze/` even though the files existed immediately afterward and `pnpm typecheck` passed. This matches the I012 tool defect pattern.
- Workflow observation: the worker had to prepend `/c/Users/jimzord12/AppData/Local/pnpm` to `PATH` before verification commands, matching prior pnpm shim environment notes.

## Verification evidence

- RED focused route/service tests failed before implementation:
  - `pnpm vitest run src/app/api/v1/assessments/analyze/analyze-service.test.ts` failed with missing `./analyze-service`.
  - `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts` failed with missing `./route`.
- Initial focused green: `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts src/app/api/v1/assessments/analyze/analyze-service.test.ts` passed (2 files / 11 tests).
- Rework focused green: `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts src/app/api/v1/assessments/analyze/analyze-service.test.ts` passed (2 files / 14 tests).
- Second rework RED: `pnpm vitest run src/app/api/v1/assessments/analyze/analyze-service.test.ts -t "rejects noncanonical narrative field ids"` failed because the arbitrary record contract allowed the noncanonical field through to safety classification.
- Second rework focused green: `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts src/app/api/v1/assessments/analyze/analyze-service.test.ts` passed (2 files / 15 tests).
- Third rework focused green: `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts src/app/api/v1/assessments/analyze/analyze-service.test.ts` passed (2 files / 15 tests) after strict response-contract parsing and unknown-key regression assertions.
- Shared-contract RED: `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts` failed because `analysisResponseSchema` and the four per-variant response schemas were not exported from `./analyze-service`.
- Shared-contract green: `pnpm vitest run src/app/api/v1/assessments/analyze/route.test.ts src/app/api/v1/assessments/analyze/analyze-service.test.ts` passed (2 files / 16 tests) after moving the route response contracts into the shared service module.
- Full gate:
  - Initial `pnpm test` passed (16 files / 149 tests).
  - Rework `pnpm test` passed (16 files / 152 tests).
  - Second rework `pnpm test` passed (16 files / 153 tests).
  - Third rework `pnpm test` passed (16 files / 153 tests).
  - Shared-contract rework `pnpm test` passed (16 files / 154 tests).
  - `pnpm typecheck` passed (`tsc --noEmit`).
  - `pnpm build` passed and listed `/api/v1/assessments/analyze` as a dynamic route.
- Required source scan equivalent for Git Bash:
  - `grep -RInE "RMP-AI-1.0|calculateNarrativeScore|review_fallback" src || true` returned expected analyze-service, domain, version, UI export, and safety-service references.

## Classification

Tool defect

## Proposed tool improvement

Fix edit-time TypeScript hook path handling for newly created Windows-native files so `write_file` and `patch` do not report TS6053 false negatives immediately after successful writes.

## Follow-up experiment

Have the reviewer Cron independently inspect the I011 diff, rerun the phase gate, and verify the strict schema/evidence tests before making the reviewer commit.
